import CompaniesConnect from './CompaniesConnect';

describe('CompaniesConnect Component', () => {
  it('connects CompaniesContainer', () => {
    expect(CompaniesConnect).toBeInstanceOf(Function);
  });
});
